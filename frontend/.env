# 前端构建/运行时的后端 API 基址
# 分开部署时改成后端实际地址，例如 http://data-tool-mcp-backend.internal:5000
# 留空则使用相对路径 /mcp-api（前后端同源部署时）
# 生产环境若经公司网关代理，应填 网关路径 + /mcp-api，例如 /data-tool-mcp/mcp-api
VITE_API_BASE=/mcp-api

# 前端 SPA 子路径（仅生产构建生效，需以 / 开头并以 / 结尾）
# 默认 /data-tool-mcp-ui/，需与 nginx.conf.template 中 location /data-tool-mcp-ui/ 保持一致
# 若网关将前端部署到其他子路径，可在此覆盖，例如 /my-app/
# VITE_BASE_PATH=/data-tool-mcp-ui/
